#include <stdio.h>
int main()
{
    int tc,i,x,n;
    int arr[1000];
    scanf("%d",&tc);
    for (i=1;i<=n;i++)
    {
        scanf("%d %d",&x,&n);
        for (;x<=n;x=x*2)
        {
            arr[]
            printf("Case %d: ",i);
            printf("%d \n",x);
        }
    }
    return 0;
}
