#include <stdio.h>

int main()
{
    int n,i,j;
    char w[200];

    scanf("%d", &n);

    for (i=1;i<=n;i++)
    {
        scanf("%s",w);
        printf("%s ",w);
        for (j=0;j<=10;j++)
        {
            w[j];
            x=
            printf("%d\n",w[j]);
        }
    }
    return 0;
}
