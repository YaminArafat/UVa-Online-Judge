#include <bits/stdc++.h>
using namespace std;
int main()
{
    int c,s;
    while(scanf("%d %d",&c,&s)==2)
    {
        int arr[n];
        for (int i=0; i<s; i++)
        {
            scanf("%d",&arr[i]);
        }
        sort(arr,arr+n);
        int k=n-1;
        for(int i=0;i<k;i++)
        {
            printf("%d: %d %d")
        }
    }
}
