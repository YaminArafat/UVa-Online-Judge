#include <bits/stdc++.h>
using namespace std;
int main()
{
    int a,b,c,x,y;
    while(scanf("%d %d %d %d",&a,&b,&c,&x,&y)==5 && a && b && c && x && y)
    {
        int arr[]={a,b,c};
        sort(arr,arr+3);
        int arr2[]={x,y};
        sort(arr,arr+2);

    }
}
