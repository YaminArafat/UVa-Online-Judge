#include <iostream>
#include <cstdio>
#include <string.h>
int main()
{
    char in[101];

    scanf("%s",in);

    x=sizeof(in);
    for (i=0;i<=x;i++)
    {
        if (in[i]=={'a','e','i','o','u'})
    }
}
